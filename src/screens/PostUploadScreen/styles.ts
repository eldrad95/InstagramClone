import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    page : {
        flex : 1
    },
    camera :{
        width : '100%',
        aspectRatio : 3/4,
    }
})

export default styles;
